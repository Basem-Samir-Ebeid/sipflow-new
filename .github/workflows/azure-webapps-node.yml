# DISABLED: This workflow has been disabled.
# The project is deployed exclusively via Vercel (not Azure).
# Evidence: @vercel/analytics, @vercel/blob, @vercel/postgres are used throughout.
# The original Azure config was the default GitHub template and was never
# configured (AZURE_WEBAPP_NAME was still set to "your-app-name").

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled — project uses Vercel only'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Workflow disabled — project uses Vercel"
